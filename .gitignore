# Model weights are never committed - fetch them with ./install.sh
models/
*.gguf
*.safetensors
*.log
*.pid
run/
outputs/
.DS_Store
__pycache__/

# env.conf backups made by ./update_env.sh - the user's, not the repo's.
env.conf.bak-*
